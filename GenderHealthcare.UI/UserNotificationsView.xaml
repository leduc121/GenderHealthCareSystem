<UserControl x:Class="GenderHealthcare.UI.Views.UserNotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Danh sách thông báo của tôi"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1"
                  x:Name="dgNotifications"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  AlternatingRowBackground="#F5F5F5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Loại"
                                    Binding="{Binding ContraceptiveType}"
                                    Width="150"/>
                <DataGridTextColumn Header="Ngày"
                                    Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}"
                                    Width="100"/>
                <DataGridTextColumn Header="Giờ"
                                    Binding="{Binding ReminderTime, StringFormat=HH:mm}"
                                    Width="80"/>
                <DataGridTextColumn Header="Tin nhắn"
                                    Binding="{Binding ReminderMessage}"
                                    Width="*"/>
                <DataGridTextColumn Header="Tần suất"
                                    Binding="{Binding Frequency}"
                                    Width="100"/>
                <DataGridTextColumn Header="Trạng thái"
                                    Binding="{Binding ReminderStatus}"
                                    Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
