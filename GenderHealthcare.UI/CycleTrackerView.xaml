<UserControl x:Class="GenderHealthcare.UI.Views.CycleTrackerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="CycleTrackerView_OnLoaded">
    <StackPanel Margin="10">
        <TextBlock Text="Theo dõi chu kỳ" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Khai báo chu kỳ -->
        <GroupBox Header="Khai báo chu kỳ" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Ngày bắt đầu:" VerticalAlignment="Center" Margin="5"/>
                <DatePicker Grid.Row="0" Grid.Column="1" x:Name="datePickerStart" Margin="5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Ngày kết thúc:" VerticalAlignment="Center" Margin="5"/>
                <DatePicker Grid.Row="1" Grid.Column="1" x:Name="datePickerEnd" Margin="5"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Ghi chú:" VerticalAlignment="Top" Margin="5"/>
                <TextBox Grid.Row="2" Grid.Column="1" x:Name="txtNotes" Height="60" TextWrapping="Wrap" Margin="5" AcceptsReturn="True"/>

                <Button Grid.Row="3" Grid.Column="1" x:Name="btnSave" Content="Lưu" Width="100" HorizontalAlignment="Left" Margin="5" Click="SaveButton_Click"/>
            </Grid>
        </GroupBox>

        <TextBlock Text="Lịch sử" FontSize="16" FontWeight="Bold" Margin="0,20,0,10"/>
        <DataGrid x:Name="cycleDataGrid" AutoGenerateColumns="False" IsReadOnly="True" Height="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ngày Bắt đầu" Binding="{Binding CycleStartDate, StringFormat=dd/MM/yyyy}" Width="*"/>
                <DataGridTextColumn Header="Ngày Kết thúc" Binding="{Binding CycleEndDate, StringFormat=dd/MM/yyyy}" Width="*"/>
                <DataGridTextColumn Header="Ghi chú" Binding="{Binding Notes}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>