<UserControl x:Class="GenderHealthcare.UI.Views.StdDiseaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <DataGrid x:Name="dgServices" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Tên bệnh" Binding="{Binding DiseaseName}"/>
                <DataGridTextColumn Header="Giá gốc" Binding="{Binding OriginalPrice}"/>
                <DataGridTextColumn Header="Offer" Binding="{Binding OfferName}"/>
                <DataGridTextColumn Header="Giảm giá" Binding="{Binding DiscountValue}"/>
                <DataGridTextColumn Header="Giá cuối" Binding="{Binding FinalPrice}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>