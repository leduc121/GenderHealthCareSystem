<UserControl x:Class="GenderHealthcare.UI.Views.PaymentManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="10">
            <Button Content="Thêm" FontFamily="Arial" Click="BtnAddPayment_Click" Margin="5"/>
            <Button Content="Cập nhật" FontFamily="Arial" Click="BtnUpdatePayment_Click" Margin="5"/>
            <Button Content="Xóa" FontFamily="Arial" Click="BtnDeletePayment_Click" Margin="5"/>
        </StackPanel>
        <DataGrid x:Name="PaymentGrid" Grid.Row="1" Margin="10" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" FontFamily="Arial" Binding="{Binding Id}" />
                <DataGridTextColumn Header="User ID" FontFamily="Arial" Binding="{Binding UserId}" />
                <DataGridTextColumn Header="Số tiền" FontFamily="Arial" Binding="{Binding Amount}" />
                <DataGridTextColumn Header="Phương thức" FontFamily="Arial" Binding="{Binding PaymentMethod}" />
                <DataGridTextColumn Header="Trạng thái" FontFamily="Arial" Binding="{Binding PaymentStatus}" />
                <DataGridTextColumn Header="Ngày thanh toán" FontFamily="Arial" Binding="{Binding PaymentDate}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>