<UserControl x:Class="GenderHealthcare.UI.Views.AppointmentBookingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <StackPanel Margin="20" VerticalAlignment="Top">
        <TextBlock Text="Chọn cố vấn:" Margin="0,0,0,5"/>
        <ComboBox x:Name="cmbConsultants"
          DisplayMemberPath="FullName"
          SelectedValuePath="Id"
            Margin="0,0,0,10"/>

            <TextBlock Text="Chọn ngày hẹn:" Margin="0,0,0,5"/>
        <DatePicker x:Name="dpDate" Margin="0,0,0,10"/>

        <TextBlock Text="Chọn giờ (HH:mm):" Margin="0,0,0,5"/>
        <TextBox x:Name="txtTime" Text="09:00" Margin="0,0,0,20"/>

        <Button Content="Đặt lịch" Width="100"
                Click="BookAppointment_Click"/>
    </StackPanel>
</UserControl>