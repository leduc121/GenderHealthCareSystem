<UserControl x:Class="GenderHealthcare.UI.Views.StdDiseaseManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="5">
            <Button Content="Thêm dịch vụ" Click="BtnAddDisease_Click" Margin="5"/>
            <Button Content="Cập nhật dịch vụ" Click="BtnUpdateDisease_Click" Margin="5"/>
            <Button Content="Xóa dịch vụ" Click="BtnDeleteDisease_Click" Margin="5"/>
        </StackPanel>
        <DataGrid x:Name="dgDiseases" Grid.Row="1" Margin="5" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Tên bệnh" Binding="{Binding DiseaseName}"/>
                <DataGridTextColumn Header="Giá xét nghiệm" Binding="{Binding TestPrice}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>