<UserControl x:Class="GenderHealthcare.UI.Views.OfferManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="5">
            <Button Content="Thêm offer" Click="BtnAddOffer_Click" Margin="5"/>
            <Button Content="Cập nhật offer" Click="BtnUpdateOffer_Click" Margin="5"/>
            <Button Content="Xóa offer" Click="BtnDeleteOffer_Click" Margin="5"/>
        </StackPanel>
        <DataGrid x:Name="dgOffers" Grid.Row="1" Margin="5" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Tên offer" Binding="{Binding OfferName}"/>
                <DataGridTextColumn Header="Phần trăm giảm" Binding="{Binding DiscountValue}"/>
                <DataGridTextColumn Header="Dịch vụ áp dụng" Binding="{Binding ApplicableServices}"/>
                <DataGridTextColumn Header="Khách hàng tiềm năng" Binding="{Binding TargetUserIds}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>