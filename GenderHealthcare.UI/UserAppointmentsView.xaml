<UserControl x:Class="GenderHealthcare.UI.Views.UserAppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
               Text="Danh sách lịch hẹn của tôi"
               FontSize="16"
               FontWeight="Bold"
               Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1"
              x:Name="dgAppointments"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              SelectionMode="Single"
              GridLinesVisibility="Horizontal"
              HeadersVisibility="Column"
              AlternatingRowBackground="#F5F5F5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Cố vấn"
                            Binding="{Binding ConsultantName}"
                            Width="150" MinWidth="120"/>
                <DataGridTextColumn Header="Ngày hẹn"
                            Binding="{Binding AppointmentDate, StringFormat=dd/MM/yyyy}"
                            Width="100" MinWidth="90"/>
                <DataGridTextColumn Header="Giờ hẹn"
                            Binding="{Binding AppointmentDate, StringFormat=HH:mm}"
                            Width="80" MinWidth="70"/>
                <DataGridTextColumn Header="Trạng thái"
                            Binding="{Binding AppointmentStatus}"
                            Width="120" MinWidth="100"/>
                <DataGridTextColumn Header="Ghi chú"
                            Binding="{Binding AppointmentLocation}"
                            Width="*" MinWidth="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
