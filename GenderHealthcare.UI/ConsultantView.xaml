<UserControl x:Class="GenderHealthcare.UI.Views.ConsultantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Profile Update Section -->
        <StackPanel Orientation="Vertical" Margin="5">
            <TextBlock Text="Cập nhật hồ sơ cố vấn" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Chuyên môn:" Margin="5"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtSpecialization" Margin="5"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Trình độ:" Margin="5"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtQualification" Margin="5"/>
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Kinh nghiệm:" Margin="5"/>
                <TextBox Grid.Row="2" Grid.Column="1" x:Name="txtExperience" Margin="5"/>
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Phí tư vấn:" Margin="5"/>
                <TextBox Grid.Row="3" Grid.Column="1" x:Name="txtConsultationFee" Margin="5"/>
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Trạng thái hồ sơ:" Margin="5"/>
                <TextBox Grid.Row="4" Grid.Column="1" x:Name="txtProfileStatus" Margin="5"/>
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Có sẵn:" Margin="5"/>
                <CheckBox Grid.Row="5" Grid.Column="1" x:Name="chkIsAvailable" Margin="5"/>
                <Button Grid.Row="6" Grid.Column="1" Content="Cập nhật" Click="BtnUpdateProfile_Click" HorizontalAlignment="Right" Margin="5"/>
            </Grid>
        </StackPanel>
        <!-- Appointments Section -->
        <DataGrid x:Name="dgAppointments" Grid.Row="1" Margin="5" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="User ID" Binding="{Binding UserId}"/>
                <DataGridTextColumn Header="Ngày hẹn" Binding="{Binding AppointmentDate, StringFormat={}{0:dd/MM/yyyy HH:mm}}"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding AppointmentStatus}"/>
                <DataGridTextColumn Header="Địa điểm" Binding="{Binding AppointmentLocation}"/>
                <DataGridTextColumn Header="Giờ bắt đầu" Binding="{Binding StartTime}"/>
                <DataGridTextColumn Header="Giờ kết thúc" Binding="{Binding EndTime}"/>
                <DataGridTextColumn Header="Giá cố định" Binding="{Binding FixedPrice, StringFormat={}{0:C}}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>